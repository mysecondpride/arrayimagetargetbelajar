<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Smartphone</title>
  </head>
  <body>
    <h2><%=data.title %></h2>
    <p><%=data.body %></p>

    <div>
      <% if (data.file && data.file.fileId) { %>
      <img
        src="/image/<%= data.file.fileId %>"
        alt="<%= data.file.filename %>"
        style="width: 300px; height: 200px; margin-top: 20px"
      />

      <% } %>
    </div>
  </body>
</html>
