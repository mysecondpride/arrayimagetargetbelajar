<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Produk Berkebun</title>
  </head>
  <body>
    <h2><%=data.title %></h2>
    <p><%=data.body %></p>
    <div>
      <% data.files.forEach(function (file) { %>
      <img src="/image/<%= file._id %>" alt="<%= file.filename%>" />
      <% }) %>
    </div>
  </body>
</html>
